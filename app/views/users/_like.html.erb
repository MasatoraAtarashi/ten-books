<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div class="row center">
    <%= hidden_field_tag :liked_id, @user.id %>
    <%= f.submit "お気に入りに登録", class: "btn", id: "favorite-text" %>
  </div>
<% end %>
